<% dummy_page_nav(title: "Methods", back_url: root_path) %>

<div class="flex w-full flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Methods",
    subtitle: "Other gems read name, logo, and favicon here. Do not call Attachable just to print a mark.",
    variant: :h1
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Read the name",
    subtitle: "RecordingStudioSiteSettings.name_for",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      root = RecordingStudio.root_recording_for(workspace)
      RecordingStudioSiteSettings.name_for(root)
    CODE
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Read the logo",
    subtitle: "RecordingStudioSiteSettings.logo_for",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      logo = RecordingStudioSiteSettings.logo_for(root)
      logo.preview_url
      logo.present?
    CODE
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Read the favicon",
    subtitle: "RecordingStudioSiteSettings.favicon_for",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      favicon = RecordingStudioSiteSettings.favicon_for(root)
      favicon.preview_url
      favicon.present?
    CODE
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Print the mark",
    subtitle: "recording_studio_site_logo",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      recording_studio_site_logo(current_root_recording)
    CODE
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Print the tab icon",
    subtitle: "recording_studio_site_favicon",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      recording_studio_site_favicon(current_root_recording)
    CODE
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Write name, logo, and favicon",
    subtitle: "RecordingStudioSiteSettings.update!",
  ) %>

  <%= render FlatPack::CodeBlock::Component.new(
    language: :ruby,
    code: <<~'CODE'
      RecordingStudioSiteSettings.update!(
        root,
        name: "Studio",
        actor: current_user,
        logo_io: File.open("logo.png"),
        filename: "logo.png",
        content_type: "image/png",
        favicon_io: File.open("favicon.png"),
        favicon_filename: "favicon.png",
        favicon_content_type: "image/png"
      )
    CODE
  ) %>
</div>
